# Copy to `.env` for local development. On Azure App Service, add the same
# variable NAMES under Configuration → Application settings (use Key Vault
# references for production secrets).
#
# Named environments: development | staging | production
#   development  — local machine (DEBUG defaults true, CORS open)
#   staging      — pre-prod host (DEBUG defaults false, CORS allowlist)
#   production   — Azure Braelo-V1 (DEBUG defaults false, CORS allowlist)
# If DJANGO_ENV is omitted, WEBSITE_SITE_NAME (Azure) implies production.

# --- Django core ---
DJANGO_ENV=development
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,.azurewebsites.net
CSRF_TRUSTED_ORIGINS=https://your-app.azurewebsites.net,http://localhost,http://127.0.0.1
# Public origin used for QR / absolute backend links. Must match the live API host.
PUBLIC_BACKEND_URL=http://127.0.0.1:8000
# Production/staging: set False and list the admin origin(s).
CORS_ALLOW_ALL_ORIGINS=True
CORS_ORIGIN_ALLOW_ALL=True
# CORS_ALLOWED_ORIGINS=https://your-admin.example.com,http://localhost:3000

# --- Google Sign-In ---
GOOGLE_OAUTH_CLIENT_ID=

# --- Firebase Admin (service account JSON, raw or a single Base64 line) ---
FIREBASE_CREDENTIALS=
# Alias:
# FIREBASE_CREDENTIALS_BASE64=
# Must match the mobile app Firebase project (google-services.json project_id).
FIREBASE_PROJECT_ID=braelo-app

# --- Azure Blob Storage ---
# Either set the full connection string from the portal, OR name + key:
AZURE_STORAGE_CONNECTION_STRING=
AZURE_ACCOUNT_NAME=
AZURE_ACCOUNT_KEY=
AZURE_CONTAINER_NAME=braelo
AZURE_CUSTOM_DOMAIN=

# --- Redis (Channels) ---
REDIS_URL=redis://127.0.0.1:6379/0

# --- Twilio ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=

# --- SMTP ---
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
# Gmail App Password only (not the account password). Strip spaces.
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# --- JWT (optional) ---
JWT_BLACKLIST_AFTER_ROTATION=True
JWT_ROTATE_REFRESH_TOKENS=True
JWT_REFRESH_TOKEN_DAYS=7
JWT_ACCESS_TOKEN_MINUTES=2880
JWT_SLIDING_REFRESH_DAYS=7
JWT_SLIDING_TOKEN_DAYS=30
JWT_SLIDING_REFRESH_LATE_DAYS=1
JWT_SLIDING_TOKEN_LATE_DAYS=30

# --- FCM (push) ---
FCM_SERVER_KEY=

# --- MongoDB ---
# Primary: full URI (Azure often uses CUSTOMCONNSTR_MONGO_URI or MONGO_URI)
MONGO_URI=
MONGO_DB_NAME=braelo
# Alternative build (requires all three):
MONGO_USERNAME=
MONGO_PASSWORD=
MONGO_ATLAS_HOST=

DJANGO_SKIP_MONGOENGINE=
# CI uses MONGO_URI or skips; see settings.py

# --- Chatbot / RAG (optional) ---
OPENAI_API_KEY=
GPT_MODEL=gpt-4o-mini
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACES_RADIUS_M=6000
GOOGLE_PLACES_MAX_RESULTS=7
EMBEDDING_MODEL=text-embedding-3-small
KNOWLEDGE_SIMILARITY_THRESHOLD=0.55
RAG_TOP_K=8
RAG_HYBRID_EMBEDDING_WEIGHT=0.58
RAG_SIMILARITY_THRESHOLD=0.32
RAG_SIMILARITY_FALLBACK=0.20
RAG_QUERY_REWRITE=true
RAG_STRONG_MATCH_HYBRID=0.68
RAG_MIN_CONFIDENT_HYBRID=0.46
RAG_EMBEDDING_MIN_FOR_MATCH=0.18
DOCX_DATA_DIR=
MAX_BUSINESS_RESULTS=5
# First response page size for directory cards (before See more / load more).
CHATBOT_DIRECTORY_FIRST_PAGE_SIZE=8
# Drop Portuguese-only listings when user asked for Brazilian (name/tags filter).
CHATBOT_BRAZILIAN_EXCLUDE_PORTUGUESE_ONLY=true
# Marketplace nearby/search radius (meters). Query param ?radius= is clamped to min/max.
LISTING_RADIUS_M=10000
LISTING_RADIUS_MIN_M=500
LISTING_RADIUS_MAX_M=100000
BUSINESS_RADIUS_MILES=25
BUSINESS_RADIUS_FALLBACK_MILES=50
MIN_BUSINESS_RESULTS=3
MONGO_BUSINESS_COLLECTIONS=business_listings,businesses
KB_PROVIDER_SUGGESTIONS=true
KB_PROVIDER_SUGGESTIONS_MAX=3
# Verbose [SessionGeo] logs in chat_flow (remembered city/state per session). false in production to reduce noise.
BRAELO_SESSION_GEO_DEBUG=true
# Agent wrapper layer (Tier 2a0): terminal logs [BraeloAgent:…]
BRAELO_AGENT_DEBUG=true
# Write learning_logs to Mongo when URI available
BRAELO_LEARNING_LOG_TO_MONGO=true

# --- Observability (Phase 10) ---
# Optional. Leave empty to disable Sentry.
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.0
# JSON stdout logs (default on when DJANGO_ENV is staging/production)
# BRAELO_JSON_LOGS=true
LOG_LEVEL=INFO
# Rate limiter: redis (production default when Redis is reachable) or memory
BRAELO_RATE_LIMIT_BACKEND=
# Business listings: if USE_MONGO is omitted and MONGO_URI is valid, Django defaults USE_MONGO=true (see settings.py).
# Set USE_MONGO=false to always use the Django Business table instead of Mongo.
USE_MONGO=false
# Marketplace listings (Vehicles, Services, …) → Mongo `businesses` mirror. Default on when MONGO_URI is valid (not tied to USE_MONGO).
# LISTINGS_DIRECTORY_MIRROR_ENABLED=true
BRAELO_MONGO_URI=
